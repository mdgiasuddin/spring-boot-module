spring:
  application:
    name: rabbitmq-consumer

  rabbitmq:
    host: localhost
    port: 5772
    username: admin
    password: admin123
    listener:
      simple:
        acknowledge-mode: auto
        default-requeue-rejected: false
        retry:
          enabled: true
          max-retries: 3
          initial-interval: 3000ms