spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/spring_test_jpa_test
    username: postgres
    password: pass_1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

